apply plugin: 'java'
apply plugin:'application'

sourceCompatibility = 1.8
version = '0.1'

mainClassName = "com.jbake.ui.Frontend"

repositories {
    mavenCentral()
}

dependencies {
    compile 'org.mapdb:mapdb:1.0.7'
    compile 'com.google.guava:guava:18.0'
}